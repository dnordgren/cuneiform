import * as React from 'react';
import * as ReactDOM from 'react-dom';

import OptionsPage from './options';

const appNode = document.getElementById('options-app');
ReactDOM.render(React.createElement(OptionsPage), appNode);
